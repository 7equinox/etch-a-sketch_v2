# etch-a-sketch_v2

Lesson Learned:
1) Use 'variables' in CSS as constant values
2) Use 'calc' for computation without giving exact val
3) Use const for constant values and query selector in js
4) Make function at least one task each
5) Create elements and class using js (as well as add and delete elem)
6) Make code as much as possible human friendly